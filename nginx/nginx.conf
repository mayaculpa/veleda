worker_processes 2;

events {
  worker_connections  1024;
}

http {
  server {
    listen 80;
    index index.html;
    server_name 127.0.0.1;

    root /usr/share/nginx/html/;
    include mime.types;
  }
}
